# 0) 기본적으로 전부 무시
/*

# 1) 루트에서 허용할 파일(화이트리스트)
!.gitignore
!README.md
!Dockerfile
!docker-compose.yml
!requirements.txt

# 2) backend 디렉터리만 통과
!/backend/

# 3) backend 내부는 기본 무시
/backend/*

# 4) 단, app만 추적
!/backend/app/
!/backend/app/**

# 5) app 내부에서도 추적 제외할 흔한 것들
/backend/app/**/__pycache__/
/backend/app/**/*.pyc
/backend/app/**/*.pyo
